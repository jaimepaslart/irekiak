Lance le serveur de développement Nuxt.

1. Vérifie que les dépendances sont installées (node_modules/ existe)
2. Si non, lance `pnpm install`
3. Lance `pnpm run dev`

Si le port 3000 est occupé, utilise le prochain port disponible.
